{
  "name": "go-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "develop": "ag test/asyncapi.yaml ./ -o test/output --force-write --watch-template"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@asyncapi/generator-filters": "^1.0.0"
  },
  "generator": {
    "parameters": null,
    "generator": ">=0.50.0 <2.0.0",
    "filters": [
      "@asyncapi/generator-filters"
    ]
  }
}
